---
type: explanation
updated: 2026-05-19
status: snapshot
---

# Documentation audit — 2026-05-19

Read-only inventory of `README.md`, `CLAUDE.md`, `CHANGELOG.md`, and `docs/**`. Produced as input to the `supaterminal-doc-bootstrap` dispatch (Lisa + Obi). Lists what exists, where it drifts from code, and what is missing. No corrective edits applied here — those land in subsequent waves.

## Inventory

| File | Lines | Type | Status | Notes |
| --- | --- | --- | --- | --- |
| `README.md` | 194 | reference (de facto) | **fresh-but-bloated** | Architecture + PTY + Permissions sections duplicate content already in `docs/architecture/*.md` |
| `CLAUDE.md` | 22 | reference (project memory) | **minimal-stub** | Only contains the 2026-05-19 worktree-bug note. Missing commands, monorepo layout, IPC convention, conventions for tests/lint |
| `CHANGELOG.md` | 55 | reference | **mixed-format** | Header cites Conventional Commits but entries do not strictly follow the prefix convention |
| `docs/concepts.md` | 36 | explanation | fresh | Canonical vocab pinned, frontmatter present (`updated: 2026-05-12`) |
| `docs/getting-started.md` | 138 | tutorial | fresh | 10-min walkthrough, well-structured, frontmatter present |
| `docs/keyboard-shortcuts.md` | 30 | reference | fresh | Frontmatter present, scope column explicit |
| `docs/how-to-manage-workspaces.md` | 57 | how-to | **misplaced** | Lives at `docs/` root while peers live under `docs/how-to/` — naming convention drift |
| `docs/how-to/configure-claude-settings.md` | (≥30) | how-to | fresh | Frontmatter present |
| `docs/how-to/grant-out-of-scope-path.md` | (≥30) | how-to | fresh | Frontmatter present |
| `docs/architecture/ipc.md` | (≥40) | explanation | fresh | Frontmatter present |
| `docs/architecture/notifications.md` | (≥40) | explanation | fresh | Frontmatter present |
| `docs/architecture/pty.md` | (≥40) | explanation | fresh | Frontmatter present |

Files scanned: 12. Status counts → fresh: 9, fresh-but-bloated: 1, minimal-stub: 1, mixed-format: 1, misplaced: 1.

## Drift findings (doc ↔ code)

| # | Severity | Location | Drift |
| --- | --- | --- | --- |
| D-1 | medium | `README.md` § Development | Claims `pnpm typecheck = tsc -b --noEmit across the monorepo`. Actual `package.json#scripts.typecheck` = `tsc -p tsconfig.json` (single project, no `-b`, no `--noEmit`). |
| D-2 | low | `README.md` § Keyboard shortcuts | Table omits `$mod + \` (cycle layout single/grid/split) which `docs/keyboard-shortcuts.md` documents and the renderer wires. |
| D-3 | low | `README.md` § Architecture | Inlines `## Architecture`, `## PTY backend`, `## Permissions model` sections that fully duplicate `docs/architecture/{ipc,notifications,pty}.md`. Two surfaces, same content → divergence risk on every code change. |
| D-4 | low | `CLAUDE.md` (project) | Silent on monorepo layout, Zod-at-boundary IPC rule, `useDesignTokens` re-render contract, conventional command set. A new Claude session needs to re-discover all of this from code. |
| D-5 | low | `CHANGELOG.md` 0.2.0 entry | Prefixes are mixed: `fix(renderer):` (conv), `feat(renderer):` (conv), `test:` (loose), `docs:` (loose), `refactor(main):` (conv). Header promises Conventional Commits — entries partially comply. |

## Broken links

None detected. The README link table to `docs/architecture/{ipc,notifications,pty}.md` and `docs/how-to/{configure-claude-settings,grant-out-of-scope-path}.md` all resolve to existing files.

## Orphan pages

None detected. Every page under `docs/**` is reachable from at least one entry point (`README.md` link table or sibling page link).

## Convention drift

| # | Surface | Drift |
| --- | --- | --- |
| C-1 | `docs/` how-to organisation | One flat (`docs/how-to-manage-workspaces.md`) vs two nested (`docs/how-to/*.md`). Pick one shape, enforce. |
| C-2 | `docs/` index | No `docs/index.md` / MOC. Discovery depends entirely on the README link table → drift here orphans the whole tree. |
| C-3 | Frontmatter shape | Every doc page carries `type:` + `updated:` (good). No declared schema document. |
| C-4 | Naming | All how-to slugs are verb-first kebab-case (`manage-workspaces`, `configure-claude-settings`, `grant-out-of-scope-path`) — convention is implicit but consistent. Worth pinning explicitly. |
| C-5 | `bug-reports/` | Folder at repo root contains only `.gitkeep` (empty). Unclear convention: archive, active triage, or dead. Move under `archive/` or document the intent. |

## Recommendations (forward references)

Each item targets the matching downstream sub-feature of `supaterminal-doc-bootstrap`:

1. **`claude-md-enrich`** — rewrite `CLAUDE.md` ≤ 150 lines, sections in stable order (commands / architecture / conventions / known-limitations). Resolves D-1 (fix the wrong typecheck command), D-4 (fill the gap). Preserve worktree-bug note verbatim.
2. **`docs-restructure`** — (a) `git mv docs/how-to-manage-workspaces.md docs/how-to/manage-workspaces.md` to resolve C-1. (b) Create `docs/index.md` as Diátaxis MOC to resolve C-2 — becomes the canonical entry point, README links to it once. (c) Create `docs/CONVENTIONS.md` to pin C-3 + C-4 (naming, frontmatter, link discipline). (d) Slim README architecture sections to a 1-line + link into `docs/architecture/*.md` to resolve D-3. Also fix D-2 (add `$mod + \` row) while editing README's shortcut table.
3. **`archive-bug-reports`** — `git mv bug-reports archive/bug-reports`, add `archive/README.md` documenting the convention. Resolves C-5.
4. **`doc-health-instrumentation`** — add `scripts/docs-health.ts` + `docs/HEALTH.md` so future drift detection is data-driven, not vibes-driven (Opti's lens). 60-day observation window before authoring a maintenance policy.
5. **`conventional-changelog`** — add `commitlint.config.js`, document Angular conventional-commits in `docs/CONVENTIONS.md`, regenerate `CHANGELOG.md` to fix D-5 entry-format drift. Husky declared opt-in only.

## Out of scope for this dispatch

- Code-signing setup (`README.md` § Code signing, marked TODO) — release-blocker only, deferred per Wave 1 council answer (c).
- `CHANGELOG.md` historical re-write — `conventional-changelog` sub-feature preserves existing entries verbatim, only future entries follow strict format.
- `apps/**` source-code comment audit — out of scope (README + docs/ + CLAUDE.md only).
